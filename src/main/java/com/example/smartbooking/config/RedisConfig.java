package com.example.smartbooking.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisConfig {

    // Redis configuration properties and beans (e.g., JedisConnectionFactory,
    // RedisTemplate)

}